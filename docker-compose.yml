version: '3.2'

services: 
  port: 
    env_file:
      - services/port/.env
      - .env
    build: 
      dockerfile: services/port/Dockerfile
      context: .
    network_mode: host
  matching-engine:
    env_file: 
      - .env
    network_mode: host
    build: 
      context: .
      dockerfile: services/matching-engine/Dockerfile

